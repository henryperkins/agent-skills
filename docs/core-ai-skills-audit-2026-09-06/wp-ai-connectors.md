# wp-ai-connectors audit (read-only) — 2026-09-06

Paths below are relative to `<upstream>/` unless absolute. `wp71` = `wp-7.1/wordpress/wp-includes/`, `gb` = `gutenberg-23.8.0/` (23.9.0 identical where cited), `sdk131` = `wp71/php-ai-client/src/`, `sdk140` = `php-ai-client-1.4.0/src/`.

## Findings

- **[high]** `SKILL.md:4` ("`application_password` needs Core 7.1 or Gutenberg 23.6+"), `SKILL.md:129` ("Gutenberg still ships its own copy under `lib/compat/wordpress-7.0/`, so a 7.0 site with a current Gutenberg gets the same surface"), `SKILL.md:136` ("On core 7.0 without Gutenberg 23.6+, the method is rejected by `register()`"), `references/provider-registration.md:61` ("also reaches 7.0 sites running Gutenberg 23.6+") → False on Core 7.0. Gutenberg's class copy is wrapped in `if ( ! class_exists( 'WP_Connector_Registry' ) )` (`gb/lib/compat/wordpress-7.0/class-wp-connector-registry.php:11`), and Core 7.0 loads its own `final` class from `wp-settings.php:297` (7.0 branch) before mu-plugins (`:498`) and plugins (`:574`). The 7.0 class accepts only `api_key`/`none` (7.0 `class-wp-connector-registry.php:199`: `must be "api_key" or "none"`; zero `application_password` hits in 7.0 `class-wp-connector-registry.php` and `connectors.php`). Gutenberg does swap the init/settings/REST *functions* (`gb/lib/compat/wordpress-7.0/default-connectors.php:79-80, 553-555, 647-648, 700-701, 782-783`), but they call Core's `register()`, which returns `null` with `_doing_it_wrong()`. Gutenberg's copy only takes effect where Core defines no class (WP 6.9), and even then only when `class_exists('\WordPress\AiClient\AiClient')` (`gb/lib/load.php:119-122`), i.e. a Composer-installed SDK. → Fix: say `application_password` requires Core 7.1; Gutenberg 23.6+ adds it only on WP 6.9 with a Composer-provided SDK, never on Core 7.0, and it is rejected on 7.0 with or without Gutenberg.
- **[medium]** `references/provider-registration.md:80` and `:210` — "Track #64789 … for expansion" (of auth methods beyond the closed list) → The Connectors dev note the skill links ties #64789 to key *encryption*: "API keys stored in the database are not encrypted but are masked… Encryption is being explored in a follow-up ticket: #64789" (fetched HTML, `scratchpad/agent-ai-connectors/devnote-connectors.html`), matching `SKILL.md:127`. Ticket itself unreachable (403), so this is a probable mis-citation. → Fix: cite #64789 only for encryption; point auth-method expansion at the dev note's "Looking ahead" section (confirmed present) without a ticket number unless verified.
- **[low]** `SKILL.md:148`, `references/provider-registration.md:210` — Akismet (`spam_filtering`) "still appears" → The route skips Akismet unless the plugin is installed: `wp71/build/routes/connectors-home/content.js:11902-11904` (`if (connectorId === "akismet" && !data.plugin?.isInstalled) continue;`). → Fix: add "when the Akismet plugin is installed".
- **[low]** `references/provider-registration.md:171` — "The SDK instantiates the provider lazily." → Providers are all-static (`sdk131/Providers/Contracts/ProviderInterface.php:27-54`); nothing is instantiated, and `registerProvider()` calls `$className::metadata()` eagerly at registration (`sdk131/Providers/ProviderRegistry.php:67`; same in `sdk140/Providers/ProviderRegistry.php:64ff`). → Fix: "the registry stores the class name and immediately calls static `metadata()` to derive the ID; no instance is created."
- **[low]** `references/provider-registration.md:107` — `modelMetadataDirectory()` = "capabilities (modalities, context window, pricing, recency)" → `ModelMetadata` holds only `id`, `name`, `supportedCapabilities`, `supportedOptions` (`sdk131/Providers/Models/DTO/ModelMetadata.php:30-49`; identical in 1.4.0). No context-window/pricing/recency fields. → Fix: "capabilities and supported options (modalities are options)".
- **[low]** `references/provider-registration.md:197-198` — `wp_get_connector()` returns "null if not registered" → It delegates to `get_registered()`, which fires `_doing_it_wrong()` before returning null (`wp71/class-wp-connector-registry.php:393-401`; `wp71/connectors.php:87-94`). → Fix: note the notice and guard with `wp_is_connector_registered()`.
- **[low]** `SKILL.md:163` — cache "keyed on provider class plus AI Client version" → Key is `'ai_client_' . AiClient::VERSION . '_' . md5(static::class)` + `models`, where `static::class` is the *model metadata directory* class (`sdk131/Providers/ApiBasedImplementation/AbstractApiBasedModelMetadataDirectory.php:33,77,93-95`; `sdk140/...:112-114`). Not-keyed-on-API-key conclusion stands. → Fix: "keyed on the metadata-directory class and AI Client version".
- **[nit]** `SKILL.md:121` — "the AI Client looks up the key in this order … 3. Database" → The SDK itself reads only env var then constant `{ID}_API_KEY` (`sdk131/Providers/ProviderRegistry.php:418-448`); the database step is Core's `_wp_connectors_pass_default_keys_to_ai_client()` at `init` 20, skipped when env/constant is set (`wp71/connectors.php:854-891`). Order/precedence correct; attribution off. → Fix: attribute step 3 to Core's connectors layer.
- **[nit]** `SKILL.md:144`, `:178`, `references/provider-registration.md:17-25` — With Gutenberg 23.8/23.9 active, `_wp_connectors_init` is removed and replaced by `_gutenberg_connectors_init`, which gates discovery on `class_exists(AiClient)` not `wp_supports_ai()` (`gb/lib/compat/wordpress-7.0/default-connectors.php:22,79-80`); net result identical (Core `register()` still returns null for `ai_provider`), but `has_action('init','_wp_connectors_init')`-style debugging misleads. → Fix: one-line note.
- **[nit]** `SKILL.md:140` — 'a "Get API key" link' → actual label "Get your API key at %s" (`wp71/js/dist/script-modules/connectors/index.js:298`). → Fix: quote the real string or describe generically.
- **[nit]** `SKILL.md:37` — quoted readme "For WordPress 6.9, the package must be installed." → actual: "For WordPress 6.9, the [wordpress/php-ai-client](…) package must be installed" (`provider-anthropic-1.0.4/readme.txt:29`; same line in Google/OpenAI). → Fix: mark as paraphrase.
- **[nit]** `SKILL.md:4` — "Gutenberg verified through: 23.8.0" → 23.9.0 is byte-identical for everything the skill relies on (see below). → Fix: bump to 23.9.0.

## Gutenberg 23.9.0 re-verification

- `diff -rq gutenberg-23.8.0/lib/compat gutenberg-23.9.0/lib/compat` → identical (includes `wordpress-7.0/{class-wp-connector-registry,connectors,default-connectors}.php`).
- `packages/e2e-tests/plugins/connectors-{application-password,test-provider,empty-state,capability-restriction,js-extensibility}.php` and `connectors-js-extensibility/` → identical between tags.
- `lib/load.php` differs only at lines 140-145 (experimental `extensible-site-editor` / `theme-preview` gating); the connectors block (`:119-122`, guarded by `class_exists('\WordPress\AiClient\AiClient')`, no version check) is unchanged. `gutenberg.php`: `Requires at least: 6.9`, `Version: 23.9.0`.
- Provenance check: PR #79403 title "Connectors: add application password settings UI", merged, milestone "Gutenberg 23.6" (GitHub HTML); `changelog.txt` lists it under `= 23.6.0 =` (header line 700, entry line 894, next header `= 23.5.3 =` at 1346). Nothing the skill relies on changed between 23.8.0 and 23.9.0.

## Provider trunk watch items

Trunk `plugin.php` versions are still 1.1.1 / 1.1.0 / 1.0.4, so everything below is unreleased (commit titles from `github.com/WordPress/<repo>/commits/trunk.atom`; sources from `raw.githubusercontent.com/.../trunk/`).

- Google trunk: "Add support for text to speech conversion (#31)" 2026-09-03 → `src/Metadata/GoogleModelMetadataDirectory.php:184-196` now declares `CapabilityEnum::textToSpeechConversion()` + `outputSpeechVoice`, `audio/wav`; new `src/Models/GoogleTextToSpeechConversionModel.php`. "Round-trip Google thought signatures and thought token usage (#26)" 2026-09-04. `composer.json` require-dev bumped to `^1.3.1`. No `GoogleEmbeddingGenerationModel.php` (404) — `SKILL.md:85` "Google 1.1.1 implements no embedding model" stays true.
- OpenAI trunk: "Add support for text to speech conversion (#42)" 2026-08-24 → `OpenAiModelMetadataDirectory.php:246-262`, new `src/Models/OpenAiTextToSpeechConversionModel.php`; "Preserve reasoning items across multi-turn Responses API calls (#26)" 2026-09-03. `composer.json` require-dev changed from `^1.4` (1.1.0 tag) to `^1.3.1` (trunk) → **`SKILL.md:85` "`wordpress/php-ai-client: ^1.4` stays in `require-dev`" becomes stale at the next OpenAI release**; the `interface_exists(EmbeddingGenerationModelInterface::class)` gating is unchanged (trunk `:189`).
- Anthropic trunk: "Feature/token limit exception (#4)" 2026-08-24 (`TokenLimitReachedException` import in `AnthropicTextGenerationModel.php:10`), "Continue paused turns (#32)" 2026-09-02 (`pause_turn`, `MAX_TOOL_CONTINUATIONS`), "Preserve extended thinking signatures" 2026-08-31 (`removeUnsignedThinkingBlocks`). `composer.json` require-dev `^1.1`.
- Lines that will need edits when releases ship: `SKILL.md:4` (verified-through versions), `SKILL.md:37,85` and `references/provider-registration.md:5,116` ("the current Anthropic 1.0.4, Google 1.1.1, and OpenAI 1.1.0 releases"), `SKILL.md:85` (`^1.4`). No skill line asserts that Google/OpenAI lack TTS, and `references/capabilities-declaration.md` carries no per-provider capability matrix (only the unchanged `CapabilityEnum` list at `:24-33`), so the TTS commits invalidate no current sentence. `capabilities-declaration.md:50-52` (Anthropic 1.0.4 sampling / `{}` tool input) is version-pinned and remains accurate.

## Verified correct

- Registry contract: `register()/unregister()/is_registered()/get_registered()/get_all_registered()` names and `?array`/`bool` returns; `/^[a-z0-9_-]+$/`; duplicate-ID and invalid-ID `_doing_it_wrong` + null; authentication rebuilt from `method` plus at most `credentials_url` (non-empty string), `setting_name`, `constant_name`, `env_var_name` (each validated non-empty string when set); auto `str_replace('-','_', "connectors_{type}_{id}_{method}")`; `none` keeps `method` only; unknown keys silently dropped — `wp71/class-wp-connector-registry.php:149-306`.
- `ai_provider` + `!wp_supports_ai()` → null without notice (`:212-215`); `set_instance()` outside `init` → `_doing_it_wrong` (`:430-441`); all 11 `@since` tags in the class are 7.0.0 (0 × 7.1.0).
- `wp_is_connector_registered/wp_get_connector/wp_get_connectors` (`wp71/connectors.php:23-161`); `_wp_connectors_init` on `init` 15 (`wp71/default-filters.php:556`): new instance → AI defaults only if `wp_supports_ai()` → Akismet (`spam_filtering`, `wordpress_api_key`, `WPCOM_API_KEY`, `https://akismet.com/get/`) → `do_action('wp_connectors_init', $registry)` (`connectors.php:216-277`); docblock example matches `SKILL.md:103-109`.
- AI discovery merge: registry name/description/logo override when non-empty, auth method always overridden, `credentials_url` kept as fallback; non-default providers get `type => 'ai_provider'`, `none` when metadata auth isn't apiKey; `setting_name = "connectors_ai_{$sanitized_id}_api_key"` (literal `ai`), `constant_name`/`env_var_name` = camelCase→CONSTANT_CASE `strtoupper(...) . '_API_KEY'`; `is_active` closure = `$ai_registry->hasProvider($id)` (`connectors.php:328-409`). Generic `register()` path gets `connectors_{type}_{id}_api_key` and no env/constant (registry `:249`).
- Logo: `_wp_connectors_resolve_ai_provider_logo_url` requires `file_exists` inside `WPMU_PLUGIN_DIR`/`WP_PLUGIN_DIR`, `plugins_url()`, else `_doing_it_wrong` (`connectors.php:175-205`); `ProviderMetadata` 7th ctor param `$logoPath` `@since 1.3.0` (`sdk131/Providers/DTO/ProviderMetadata.php:74,85`; `sdk140/...:84,95-102`); flagships gate on `version_compare(AiClient::VERSION,'1.3.0','>=')` (`provider-anthropic-1.0.4/src/Provider/AnthropicProvider.php:82-83`, Google `:97-98`, OpenAI `:102-103`).
- Application password (Core 7.1): `@since 7.1.0` on `wp_connectors_parse/get/sanitize_application_password_credentials` (`connectors.php:476,514,632`); first-colon split with trim (`:484-488`); unparsable non-empty env/constant → `_doing_it_wrong` and fall-through (`:534-543,557-565`); DB array `username`/`password` (`:570-578`); `type => 'object'` setting with schema and `sanitize_callback` (`:804-842`); 16 × U+2022 mask on every `/wp/v2/settings` response (`:712-721`); masked resubmit keeps stored password (`:665-667`); empty username discards both (`:669-674`); not validated (`:687`, `:720`). e2e fixture uses `content_source`, `application_password`, `credentials_url` = remote `wp-admin/profile.php`, env value `env-remote-user:abcd efgh ijkl mnop 1234` (`gb/packages/e2e-tests/plugins/connectors-application-password.php:15-45`).
- Key-save path: `sanitize_callback => 'sanitize_text_field'` (`connectors.php:801`); `add_filter('rest_post_dispatch', ..., 10, 3)` (`:753`); route `/wp/v2/settings`, `POST`/`PUT`, `'ai_provider' === type`, `true !== _wp_connectors_is_ai_api_key_valid()` → `update_option($setting_name, '')` and blanked response (`:697-752`); validator sets `ApiKeyRequestAuthentication` then `isProviderConfigured()`, returns null + `wp_trigger_error` on exception (`:591-618`); `wp_trigger_error` bails unless `WP_DEBUG` (`wp71/functions.php:6222-6223`); no `WP_Error`/`add_settings_error`.
- `isProviderConfigured()` → `$className::availability()->isConfigured()` (`sdk131/Providers/ProviderRegistry.php:184-195`); `createProviderAvailability()` abstract (`sdk131/Providers/AbstractProvider.php:111`); `ListModelsApiBasedProviderAvailability::isConfigured()` = `listModelMetadata()` in try/`catch (Exception)` (`:41-52`); `GenerateTextApiBasedProviderAvailability` sets `KEY_MAX_TOKENS => 1`, sends `'a'` (`:48-62`); `NetworkException extends RuntimeException extends \RuntimeException` (`sdk131/Providers/Http/Exception/NetworkException.php:17`), thrown by `wp71/ai-client/adapters/class-wp-ai-client-http-client.php:82,114`; list-models cached 86400 (`AbstractApiBasedModelMetadataDirectory.php:77`); `WP_AI_Client_Cache` is `wp_cache_get/set` (`wp71/ai-client/adapters/class-wp-ai-client-cache.php:65,87`), so non-persistent without an object-cache drop-in. All three flagships use `ListModelsApiBasedProviderAvailability` (Anthropic `:97`, Google `:112`, OpenAI `:117`).
- Client alert: thrown when the returned key is empty or unchanged (`content.js:11405-11408`); rendered as `<span role="alert" className="connector-settings__error">` (`wp71/js/dist/script-modules/connectors/index.js:291,323`; source `gb/packages/connectors/src/connector-item.tsx:236,275`).
- Card rules: `render = ApiKeyConnector` for `api_key`, `ApplicationPasswordConnector` for `application_password`, nothing otherwise, unless a plugin pre-registered `render` (`content.js:11898-11925`); page filters to `connector.render` (`:12176-12178`, `:12248`); `none` → no card; `wp_get_connector()` still returns it.
- SDK provider contract (1.3.1 and 1.4.0): `ProviderInterface` static `metadata()/model(string, ?ModelConfig)/availability()/modelMetadataDirectory()`; `AbstractProvider` `final` publics + abstract `createModel(ModelMetadata, ProviderMetadata)/createProviderMetadata()/createProviderAvailability()/createModelMetadataDirectory()`; `model()` applies config after construction (`sdk131/Providers/AbstractProvider.php:37-119`; `sdk140/...:42-135`); `AbstractApiProvider::baseUrl()` (`:28`/`:30`); `ProviderRegistry::registerProvider(string)/hasProvider/getRegisteredProviderIds/getProviderClassName/isProviderConfigured/setProviderRequestAuthentication/getProviderRequestAuthentication` (`sdk131:58-341`; `sdk140:64-414`); `AiClient::VERSION` `'1.3.1'` (`sdk131/AiClient.php:87`) / `'1.4.0'` (`sdk140/AiClient.php:93`). Bootstrap sample compiles against both.
- Enums/DTOs: `CapabilityEnum` = exactly the 8 cases listed at `capabilities-declaration.md:24-33` in both versions; `OptionEnum::dimensions()` and `ModelConfig::KEY_DIMENSIONS` exist only in 1.4.0 (`sdk140/Providers/Models/Enums/OptionEnum.php:24`, `sdk140/Providers/Models/DTO/ModelConfig.php:76`; absent in 1.3.1); no `json_response`/`system_instruction`/`function_calling`/`streaming` cases; `SupportedOption(OptionEnum $name, ?array $supportedValues = null)`; `ModelMetadata(string, string, array, array)`; array option values compared as normalized sets, exact combination match (`sdk131/Providers/Models/DTO/SupportedOption.php:74-100`).
- Embeddings (1.4.0 only): `EmbeddingGenerationModelInterface` declares only `generateEmbeddingResult(array $inputs): EmbeddingResult`, no `extends`; `ModelRequirements::fromEmbeddingData()` (`:216-244`); `ModelConfig::getDimensions()` (`:804`); `EmbeddingResult` requires ≥1 vector, dimensions ≥1, matching lengths (`Results/DTO/EmbeddingResult.php:103-115`); `EmbeddingBuilder` rejects count mismatch (`Builders/EmbeddingBuilder.php:193-198`), `usingDimensions()`/`isSupported()` (`:137,150`); `AiClient::input()` (`AiClient.php:259`); no `EmbeddingOperation`/`EmbeddingGenerationOperationModelInterface`. Bundled 1.3.1 has no `Providers/Models/EmbeddingGeneration/`, `Results/DTO` = Candidate/GenerativeAiResult/TokenUsage, `Builders` = MessageBuilder/PromptBuilder; `wp-settings.php:291` autoloads the SDK before plugins (`:506`, `:582`).
- Provider plugins at tags: headers `Requires at least: 6.9`, `Requires PHP: 7.4`, `add_action('init', ..., 5)`, `class_exists` + `hasProvider` + `registerProvider` (`plugin.php:41-54` in all three); `wordpress/php-ai-client` only in `require-dev` (+ `suggest` text exactly as quoted); `/vendor` in `.distignore`; `deploy-to-wporg.yml` = checkout + `10up/action-wordpress-plugin-deploy` only; `src/autoload.php` is an `spl_autoload_register` PSR-4 shim for the plugin namespace; OpenAI 1.1.0 `interface_exists(EmbeddingGenerationModelInterface::class)` gates `embeddingGeneration()`/`dimensions()` (`src/Metadata/OpenAiModelMetadataDirectory.php:189-198`) and `OpenAiEmbeddingGenerationModel` `@since 1.1.0` (`src/Models/OpenAiEmbeddingGenerationModel.php:24-30`); Anthropic/Google ship no embedding model; Anthropic 1.0.4 drops temperature/topP/topK for Opus ≥4.7 and any major ≥5 (`src/Metadata/AnthropicModelMetadataDirectory.php:103-189`) and converts null/empty tool args to `stdClass` outbound, empty back to null inbound (`src/Models/AnthropicTextGenerationModel.php:328-338,563-570`); all three sort models via `usort(..., 'modelSortCallback')` (Anthropic `:144`, Google `:259`, OpenAI `:363`).
- WP layer: `using_model_preference()`/`is_supported_for_text_generation()` via `__call` snake→camel (`wp71/ai-client/class-wp-ai-client-prompt-builder.php:58,83,293`); preference miss falls back to first candidate (`sdk131/Builders/PromptBuilder.php:1169-1184`); `GenerativeAiResult::getProviderMetadata()/getModelMetadata()` (`:131,142`); `RequestAuthenticationMethod::apiKey()` (`sdk131/Providers/Http/Enums/RequestAuthenticationMethod.php:15,23`); `wp_supports_ai()` = `WP_AI_SUPPORT` + filter (`wp71/ai-client.php:21-38`).
- AI plugin: Key Encryption experiment added in 1.1.0 (`ai-1.3.0/CHANGELOG.md:131-134`), encrypts every `connectors_ai_*_api_key` option through bundled libsodium secrets API, restores plaintext on opt-out/deactivation (`ai-1.3.0/includes/Experiments/Key_Encryption/Key_Encryption.php:22-25,89`; `docs/experiments/key-encryption.md`).
- Community: Fueled/ai-provider-for-ollama (`develop`, v1.2.0, Requires 7.0): `RequestAuthenticationMethod::apiKey()` (`includes/Provider/OllamaProvider.php:90`); host = `getenv('OLLAMA_HOST')` → `Settings > Ollama` option → `http://localhost:11434`, then `putenv()` (`includes/Plugin.php:50-79`); `baseUrl()` reads `OLLAMA_HOST` (`OllamaProvider.php:36-42`); `api/tags` + per-model `api/show` (`includes/Metadata/OllamaModelMetadataDirectory.php:42,236`); settings page text covers Ollama Cloud + `OLLAMA_HOST` (`includes/Settings/OllamaSettings.php:93,142-154`); empty-key fallback auth at `init` 15 (`Plugin.php:111-132`). Call-for-testing post links Fueled Ollama, `jonathanbossenger/wp-openrouter-provider`, `saarnilauri/ai-provider-for-mistral`. All dev-note, wordpress.org and GitHub URLs in `SKILL.md:192-202` return 200; dev note contains "Looking ahead".

## Could not verify

- Trac #64789 body/title: `core.trac.wordpress.org` returns a JS browser-check (curl), 403 (WebFetch), 403 (headless browser). Only the dev note's framing ("Encryption is being explored in a follow-up ticket: #64789") is available.
- That Core 7.1's PHP implementation "reached core through" Gutenberg PR #79403: the PR (settings UI + compat, milestone 23.6) and the 23.6.0 changelog entry are confirmed; the Core changeset/ticket linkage was not checked.
- Gutenberg 23.6.x file contents at that tag (only 23.8.0/23.9.0 clones on disk); 23.6.0 changelog entry is the evidence for "introduced in 23.6".
- OpenRouter and Mistral community-plugin internals (`references/community-providers.md:7-35`): pattern-level prose, not source-checked (GitHub API rate-limited; only Ollama was fetched raw).
- Runtime behaviour statements (persistent object cache making the key check non-live; timeout blanking a valid key) are derived from code reading, not executed against a live site.
