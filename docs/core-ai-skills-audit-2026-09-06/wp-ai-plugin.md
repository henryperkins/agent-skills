# wp-ai-plugin skill audit — against WordPress/ai 1.3.0 (tag fc115ba), WP 7.1, Gutenberg v23.8.0/v23.9.0, MCP Adapter 0.6.1

Paths below: `ai-1.3.0/` = WordPress/ai tag 1.3.0; `wp-7.1/` = WordPress 7.1 package; `gb-23.8.0/` = Gutenberg v23.8.0; `develop/` = WordPress/ai `develop` @ 2026-09-06 (Version header still 1.3.0).

## Findings

- **[high]** `SKILL.md:196`, `SKILL.md:194`, `SKILL.md:260`, `SKILL.md:269`, `references/guidelines-integration.md:3,16,77` — "AI plugin 1.3.0 still reads Gutenberg's `wp_guideline` CPT … Gutenberg 23.8.0 carries the newer Knowledge model … only under experimental plugin code" / "stored as a `wp_guideline` custom post type provided by Gutenberg 23.0+" / verification step "edits to the site Guidelines (in Gutenberg's `wp_guideline` CPT) change the system instruction" → Gutenberg removed `wp_guideline` in **23.6.0**: `gb-23.9.0/changelog.txt:1551-1552` (inside the `= 23.6.0 =` section starting at :1153) "Knowledge: Dissolve the Guidelines singleton into per-scope rows (#79263)" and "Knowledge: Rename the Guidelines CPT storage primitive to Knowledge (#79149)"; no `register_post_type( 'wp_guideline'` exists anywhere in the 23.8.0 or 23.9.0 checkouts (grep of `lib/**`), only `wp_knowledge` (`gb-23.8.0/lib/experimental/knowledge/class-gutenberg-knowledge-post-type.php:22,51`), loaded solely when `gutenberg-guidelines` is enabled (`gb-23.8.0/lib/load.php:229-231`). AI 1.3.0 hard-codes `POST_TYPE = 'wp_guideline'` (`ai-1.3.0/includes/Services/Guidelines.php:33`) and `is_available()` is `post_type_exists( self::POST_TYPE )` (:147-149), so on Gutenberg ≥ 23.6.0 `should_use_guidelines()` is false, `format_for_prompt()` returns `''`, and no `<guidelines>` block is ever appended — the skill's "verified through 23.8.0" verification step cannot pass. Upstream confirms: PR #988 (merged 2026-09-01, unreleased) "reads … from the shared `wp_knowledge` post type instead of the removed `wp_guideline` post type … prompt-building integrations quietly stopped receiving guidelines." → Say plainly that AI 1.3.0 Guidelines work only with Gutenberg 23.0–23.5.x, are a silent no-op on 23.6.0+ (incl. 23.8.0/23.9.0), add that as a failure mode, and point to #988 as the pending fix.
- **[medium]** `references/hooks-and-filters.md:7` — "renamed the family from `AIE_*` to `WPAI_*` (#317)" → the 0.5.0 constants were `AI_EXPERIMENTS_*`: `AI_EXPERIMENTS_DIR` (0.5.0 `ai.php:32`) plus `AI_EXPERIMENTS_VERSION`, `AI_EXPERIMENTS_PLUGIN_FILE`, `AI_EXPERIMENTS_PLUGIN_DIR`, `AI_EXPERIMENTS_PLUGIN_URL`, `AI_EXPERIMENTS_MIN_PHP_VERSION`, `AI_EXPERIMENTS_MIN_WP_VERSION`, `AI_EXPERIMENTS_DEFAULT_ABILITY_CATEGORY` (0.5.0 `includes/bootstrap.php:26-44`); the string `AIE_` appears nowhere in 0.5.0 or 1.3.0; PR #317 is titled "dev!: rename plugin constants to `WPAI_*`". → Replace `AIE_*` with `AI_EXPERIMENTS_*`.
- **[medium]** `SKILL.md:227`, `references/hooks-and-filters.md:174` — "(`wp_ai_client_default_request_timeout` is most likely the core AI Client's own timeout filter; verify against the installed core version)" → It is definitively core: `wp-7.1/wp-includes/ai-client/class-wp-ai-client-prompt-builder.php:194-215` applies `apply_filters( 'wp_ai_client_default_request_timeout', $default_timeout )` (`@since 7.0.0`, one `float` arg, default `30.0`, non-negative numeric or `_doing_it_wrong`) in the `WP_AI_Client_Prompt_Builder` constructor via `usingRequestOptions()`. The plugin then overrides that per request for image generation with `RequestOptions::setTimeout( get_default_request_timeout( 'image-generation', 90 ) )` (`ai-1.3.0/includes/Abilities/Image/Generate_Image.php:251-254`), i.e. `wpai_default_request_timeout` wins for image generation and core's filter governs everything else. The "appears in no plugin PHP" claim is correct (repo-wide grep empty). → Drop the hedge; state core's 7.0+ filter signature and the 90 s image-generation default.
- **[low]** `SKILL.md:225`, `references/hooks-and-filters.md:195` — "`wpai_min_content_length` (default 250 characters, per feature)" → 250 is only the helper's parameter default (`ai-1.3.0/includes/helpers.php:705`); callers pass their own: Editorial Notes 75 (`includes/Experiments/Editorial_Notes/Editorial_Notes.php:157`), Content Resizing 25 (`includes/Abilities/Content_Resizing/Content_Resizing.php:42`), Content Translation 5 (`includes/Abilities/Content_Translation/Content_Translation.php:35`). → Say "250 for most features; 75/25/5 for Editorial Notes/Content Resizing/Content Translation; second arg is the feature id".
- **[low]** `references/guidelines-integration.md:16-25` — describes the storage as one post with four meta keys → 1.3.0's service additionally constrains the query to the `wp_guideline_type` taxonomy term `content` when that taxonomy exists ("Gutenberg 23.1+"), and queries `publish`+`draft` newest-first (`ai-1.3.0/includes/Services/Guidelines.php:36-51, 325-346`); without that clause the newest artifact guideline would shadow the content singleton. → Mention the `wp_guideline_type`/`content` filter (moot once #988 ships; see watch items).
- **[low]** `SKILL.md:53`, `references/hooks-and-filters.md:39` — "uses this gate internally before initializing experiments (PR #268)" → the only `wp_supports_ai()` call is the `ai_support` requirement (`ai-1.3.0/includes/Requirements.php:120-123`) evaluated in `Main::load()` on `plugins_loaded` (`includes/Main.php:78-82`); when it fails the whole plugin aborts, so `includes/helpers.php` is never required and every `WordPress\AI\*` helper is undefined. → Note that the gate short-circuits the entire plugin (helpers included), not just experiments.
- **[low]** `references/experiments-framework.md:204` — "AI-authored code requires explicit disclosure per the AI Authorship guidelines" → the requirement lives in `ai-1.3.0/.github/PULL_REQUEST_TEMPLATE.md:16-22` ("Use of AI Tools … you must disclose … See the WordPress AI Guidelines: https://make.wordpress.org/ai/handbook/ai-guidelines/"); `CONTRIBUTING.md` has no such section and nothing is named "AI Authorship guidelines". → Point at the PR template and the WordPress AI Guidelines handbook page.
- **[low]** `SKILL.md:228`, `references/hooks-and-filters.md:177`, `references/experiments-framework.md:108` — "`wpai_feature_{$id}_settings` … (the v1.3.0 Type Ahead Feature does)" → the hook is `@since 1.1.0` (`ai-1.3.0/includes/Experiments/Type_Ahead/Type_Ahead.php:145-150`) and Type Ahead is an Experiment (`includes/Experiments/Experiments.php:48`), not a stable Feature. → "Type Ahead Experiment, since 1.1.0".
- **[nit]** `references/hooks-and-filters.md:176` — `wpai_settings_feature_metadata` documented with one value → also receives `$registry` (`ai-1.3.0/includes/Settings/Settings_Page.php:335`). → Add `( array $metadata, Registry $registry )`.
- **[nit]** `references/hooks-and-filters.md:196` — `wpai_has_image_generation_support` "auto-detected bool" → second arg `$connectors` (`ai-1.3.0/includes/helpers.php:570`). → Add `( bool $has_support, array $connectors )`.
- **[nit]** `references/guidelines-integration.md:49` — preamble quoted as "…the site's editorial standards…" → the literal string is `site&#039;s` (HTML entity, sent to the model as-is) (`ai-1.3.0/includes/Abstracts/Abstract_Ability.php:181`); image generation uses a different, shorter preamble in the *prompt* (`includes/Abilities/Image/Generate_Image.php:255-258`). → Quote verbatim and note the image-generation variant.
- **[nit]** `SKILL.md:142-160` — "From `includes/Experiments/Title_Generation/Title_Generation.php`" → the file uses `Title_Generation_Ability::class` and also hooks `admin_enqueue_scripts` (`ai-1.3.0/includes/Experiments/Title_Generation/Title_Generation.php:53-72`); the sample substitutes `My_Internal_Linker_Ability::class`. → Label the block "adapted from".
- **[nit]** `SKILL.md:242` — "Only then does `Gated_Abilities::get_all()` register the five … and run the `show_in_abilities` polyfill" → `get_all()` only instantiates wrappers (`ai-1.3.0/includes/Abilities/Gated/Gated_Abilities.php:50-107`); `Custom_Abilities::register()` runs the polyfill and each wrapper's `register()` (`includes/Experiments/Custom_Abilities/Custom_Abilities.php:56-66`). `SKILL.md:175` already has it right. → Align :242 with :175.
- **[nit]** `references/hooks-and-filters.md:275` — "`Loader::register_features()` runs once per request, early in the bootstrap" → it runs inside `Main::initialize_features()` on `init` priority 15 (`ai-1.3.0/includes/Main.php:97`). → "on `init` 15".
- **[nit]** `references/experiments-framework.md:120` — "#747, merged 2026-07-29" → GitHub `merged_at` 2026-07-28T20:26:33Z. → Use 2026-07-28 (UTC).
- **[nit]** `references/dashboard-widgets.md:96` — "the orchestrator (~80 lines)" → 90 lines (`wc -l ai-1.3.0/includes/Admin/Dashboard/Dashboard_Widgets.php`). → "~90 lines".
- **[nit]** `SKILL.md:250` — "because the embedded 1.4 surface was not ready" → `ai-1.3.0/ai.php:78-83` says only that upstream embeddings changes "will need to be pulled in … will contain some breaking changes"; no version is named (PR #975 ties it to php-ai-client#274 and #946). → Quote the comment instead of "1.4".

## Hook inventory diff

Method: multi-line-aware regex over `ai.php`, `uninstall.php`, `includes/**/*.php` at tag 1.3.0 (`apply_filters`, `apply_filters_deprecated`, `do_action`, `do_action_deprecated`; dynamic names normalised; `apply_filters_ref_array`/`do_action_ref_array` absent).

**Totals at 1.3.0:** **61 distinct `wpai_*` hook names** = 56 active filters + 2 deprecated-only filters (`wpai_summarization_min_content_length` `Summarization.php:196`, `wpai_meta_description_result_temperature` `Meta_Description.php:308`) + 3 actions (`wpai_register_features` `Loader.php:96`, `wpai_features_initialized` `Loader.php:208`, `wpai_request_logged` `AI_Request_Log_Repository.php:131`). Plus **10 legacy `ai_experiments_*`** names (8 `apply_filters_deprecated` incl. `ai_experiments_experiment_{$id}_enabled` in `Abstract_Feature.php:182`; 2 `do_action_deprecated` in `Deprecated.php:169,187`). Plus 16 non-plugin names: 4 core hooks re-applied (`the_content`, `the_excerpt`, `get_the_excerpt`, `http_request_host_is_external`) and 12 vendored `secrets_*` hooks in `includes/Vendor/Secrets/`. SKILL.md's "five request-log filters plus `wpai_request_logged`" and "ten legacy names, two actions" both match.

**In source but not in hooks-and-filters.md:**
- `wpai_pre_has_valid_credentials_check` — `ai-1.3.0/includes/helpers.php:612`, `apply_filters( 'wpai_pre_has_valid_credentials_check', null )` in `has_valid_ai_credentials()` (`@since 0.1.0`; non-null return short-circuits the live `is_supported_for_text_generation()` probe). Appears only as the replacement name in the deprecated table (:76), never as its own row.
- (Out of scope but present) the 12 vendored `secrets_*` hooks (`secrets_provider`, `secrets_access`, `secrets_pre_get`, `secrets_pre_set`, `secrets_accessed`, `secrets_{$operation}`, `secrets_access_denied`, `secrets_post_set`, `secrets_post_delete`, `secrets_provider_registered`, `secrets_provider_selected`, `secrets_master_key_rotated`) in `includes/Vendor/Secrets/`; 1.3.0's changelog (`CHANGELOG.md:64`) even documents a signature change to `secrets_accessed`/`secrets_{$operation}`.

**In hooks-and-filters.md but not in source / mis-described:** no phantom hooks; every named hook exists and every cited `file:line` is correct (`helpers.php:227/300/342`, `Log_Data_Extractor.php:77/179/257/318`, `AI_Request_Log_Manager.php:113`, `Comment_Analysis.php:205/230`, `Comment_Moderation.php:406/498`, `Content_Classification.php` Experiments `:284/:309` and Abilities `:400/:440`, `Meta_Description.php` `:209`/`:262`, `SEO_Integration.php:67/126`, `Ability_Handler.php:152`, `Posts.php:305/382`, `Import_Base64_Image.php:278`). Mis-descriptions: `wpai_settings_feature_metadata` (missing `$registry`), `wpai_has_image_generation_support` (missing `$connectors`), `wpai_min_content_length` ("default 250" is caller-dependent), `wpai_feature_{$id}_settings` (since 1.1.0, Experiment) — see Findings.

## WordPress/ai develop (unreleased) watch items

Develop hook set = 1.3.0 set **+1** (`wpai_comment_analysis_post_context_shareable`); nothing removed; `Experiments::EXPERIMENT_CLASSES` and `Gated_Abilities::GATED_ABILITY_CLASSES` byte-identical to 1.3.0; `CHANGELOG.md` "Unreleased" still empty.

- **#988 "Fix: Read Guidelines from wp_knowledge storage"** (merged 2026-09-01; `includes/Services/Guidelines.php` +tests +docs): `POST_TYPE = 'wp_knowledge'`, `TAXONOMY = 'wp_knowledge_type'`, new `TERM_GUIDELINE = 'guideline'`; rows read by slug — `guideline-{scope}` via new `Guidelines::scope_slug()` and `guideline-block-{namespace}_{name}` via new `Guidelines::block_slug()` (`str_replace( '/', '_' )`); **published rows only**; scope list comes from `wp_guideline_scopes()` when defined (minus `blocks`), else the built-in `site/copy/images/additional`; `wpai_max_guideline_length` now filters `wp_guideline_max_length()` when available (else 5000); separate per-block cache; `Abstract_Ability::get_guidelines_for_prompt()` still whitelists only `site/copy/images/additional` (`develop/includes/Abstracts/Abstract_Ability.php:139-144`). Will invalidate: `SKILL.md:194,196,260,269,290`; `references/guidelines-integration.md:3,5-12,14-25,47-48,77,99,152,158`; `references/hooks-and-filters.md:168` (default becomes "5000 or `wp_guideline_max_length()`"). Note the `@since x.x.x` placeholders on the new members.
- **#975 "Bring over latest embeddings code from the PHP AI Client"** (merged 2026-08-31): `ai.php:78` now calls `\WordPress\AI\SDK_Overlay::register();` (comment block gone); `generate_embeddings()` requires `model` (`ai_embeddings_missing_model`) and, for a model-ID string, `provider` (`ai_embeddings_missing_provider`) (`develop/includes/helpers.php:847-870`); `supports_embedding_generation()` is unchanged in form (`:823-825`) but on stock WP 7.1 — which bundles PHP AI Client **1.3.1** without `EmbeddingBuilder` (`wp-7.1/wp-includes/php-ai-client/src/AiClient.php:87`; grep for `EmbeddingBuilder` in `wp-includes/` empty) — the overlay's `embeddings` feature activates (`develop/includes/SDK_Overlay.php:86-110`, `decide()` returns `activate` when the sentinel is absent), so it flips to **true**. New temporary `includes/CLI/Embeddings_Command.php` (`wp ai embeddings generate`). Vendored trait renamed `ModelResolutionTrait` → `ModelConfigurationTrait`. Will invalidate: `SKILL.md:250,262,272`; `references/hooks-and-filters.md:50`.
- **#976 "Add a portable storage and CRUD layer for embeddings"** (merged 2026-09-02): new `includes/Embeddings/` (`Embedding_Schema` → `wpai_embeddings` table, `Embedding_Record`, `Embedding_Repository`, `Embedding_Repository_Interface`, `Vector_Codec`); `Admin/Uninstall.php` drops the table. New surface for the inventory; widens what `wpai_remove_data_on_uninstall` protects.
- **#1005 "Import core's prefixed PSR EventDispatcher in the vendored EmbeddingBuilder"** (merged 2026-09-04): vendoring fix + `SDK_OverlayTest` guard; no skill text affected.
- **#681 "Feat 514: add comment value score"**: new filter `wpai_comment_analysis_post_context_shareable( bool $shareable, WP_Post $post )` (`develop/includes/Abilities/Comment_Moderation/Comment_Analysis.php:290`, `@since x.x.x`); the default `wpai_comment_analysis_response_schema` now requires `value_score` and a `wpai_comment_analysis_result` short-circuit array must carry `value_score` (`:195-204`, `:149` `META_VALUE_SCORE`). Affects `references/hooks-and-filters.md:127-128` and the 61-hook count (→ 62).
- **#972 "Limit comment moderation bulk analysis queue size"**: bulk analysis now capped by `get_bulk_action_max_items( 'comment-moderation' )` with a `wpai_analysis_truncated` notice (`develop/includes/Experiments/Comment_Moderation/Comment_Moderation.php:1115-1129`), so `wpai_bulk_action_max_items` gains a third feature id. Affects `references/hooks-and-filters.md:178`.
- **#990 "Add/connector docs"**: docs only.

## Gutenberg 23.9.0 re-verification

- `lib/experimental/knowledge/` is byte-identical between v23.8.0 and v23.9.0 (`diff -rq` clean; files: `class-gutenberg-guideline-scopes-rest-controller.php`, `class-gutenberg-knowledge-post-type.php`, `class-gutenberg-knowledge-rest-controller.php`, `index.php`, `knowledge.php`, `load.php`).
- `lib/experimental/experiments/load.php` diff is one string: the `gutenberg-media-editor` description (`:48-51`). The `gutenberg-guidelines` experiment (`id`, label "Guidelines", description "Enables the Guidelines page under Settings and the experimental knowledge storage (wp_knowledge).") is identical at `:130-132` in both tags.
- `lib/load.php` gate `if ( gutenberg_is_experiment_enabled( 'gutenberg-guidelines' ) ) { require …/knowledge/load.php; require …/knowledge/index.php; }` is unchanged (`23.8.0:229-231`, `23.9.0:229-231`); the only `lib/load.php` change is extensible-site-editor gating (`:140-145`).
- `lib/compat/wordpress-7.0` and `lib/compat/wordpress-7.1` contain no knowledge/guideline code in either tag; WP 7.1 core ships neither `wp_guideline` nor `wp_knowledge` (grep of `wp-includes/` + `wp-admin/` empty).
- Skill claims about the Knowledge model verified against `gb-23.8.0/lib/experimental/knowledge/knowledge.php`: `wp_knowledge_types()` filter with `guideline`/`memory`/`note` (:29-58), `wp_guideline_scopes()` filter shipping `site/copy/images/blocks/additional` (:90-127), per-scope `guideline-{scope}` rows and `guideline-block-*` (:66-73), `wp_guideline_max_length()` default 5000 filter `wp_guideline_max_length` (:135-141), REST `/wp/v2/knowledge` (`class-gutenberg-knowledge-post-type.php:81-82` `rest_base 'knowledge'`) and `/wp/v2/knowledge/guideline-scopes` (`class-gutenberg-guideline-scopes-rest-controller.php:28`).
- Net: "Gutenberg verified through: 23.8.0" can be bumped to 23.9.0 with no wording change — but the high finding above applies to both.

## Verified correct

- Constants/bootstrap: five `WPAI_*` constants incl. `WPAI_VERSION '1.3.0'`, `WPAI_DEFAULT_ABILITY_CATEGORY 'ai-experiments'` — `ai-1.3.0/ai.php:38-68`; `// \WordPress\AI\SDK_Overlay::register();` with the quoted rationale — `ai.php:78-85`; `Main::get_instance()` singleton — `ai.php:87`, `includes/Main.php:50-57`.
- Registration flow: `init` priority 15 — `includes/Main.php:97`; `Loader::init()` → `register_features()` → `initialize_features()` — `includes/Features/Loader.php:61-64`; `wpai_default_feature_classes` in `get_default_features()` :120; `wpai_register_features` :96; `wpai_features_enabled` default `true` :186; `is_enabled()` skip loop :193-197; `wpai_features_initialized` :208; `Registry::register_feature()` false on duplicate — `includes/Features/Registry.php:42-51`; `Experiments::init()` priority 9 — `includes/Experiments/Experiments.php:58`.
- Experiments inventory: 19 `EXPERIMENT_CLASSES` in the skill's order — `Experiments.php:29-49`; `Example_Experiment` unregistered; Custom Abilities id/category/`'capability' => 'none'` — `includes/Experiments/Custom_Abilities/Custom_Abilities.php:34-47`; `GATED_ABILITY_CLASSES` = `Post_Utilities, Read_Settings, Read_Users, Read_Content` and `wpai_gated_abilities` with `_doing_it_wrong` on invalid — `includes/Abilities/Gated/Gated_Abilities.php:36-41,61-87`; the five IDs — `Content.php:203`, `Settings.php:134`, `Users.php:127`, `Posts.php:59,136`; changelog #881 — `CHANGELOG.md:11`; 1.2.0 registered them unconditionally from `Main` — 1.2.0 `includes/Main.php:137-144`.
- `Abstract_Feature` contract: `final __construct` with defaults `category→Feature_Category::OTHER`, `stability→'experimental'`, `image→''`, `capability→'text_generation'`, `InvalidArgumentException` on missing label/description — `includes/Abstracts/Abstract_Feature.php:96-127`; `final is_enabled()/is_globally_enabled()/is_individually_enabled()` reading `wpai_features_enabled` / `wpai_feature_{$id}_enabled` options (both `register_setting` default `false` — `includes/Settings/Settings_Registration.php:50,87-112`) — `:171-219`; `get_field_option_name()` → `wpai_feature_{id}_field_{name}` — `:318-320`; `Contracts\Feature` 12 methods, `get_capability()` `@since 0.9.0`, `is_*_enabled()` `@since 1.0.1` — `includes/Contracts/Feature.php`; `Experiment_Category::EDITOR/ADMIN` = `'editor'/'admin'` extending `Feature_Category::OTHER = 'other'` — `Experiment_Category.php:29-51`, `Feature_Category.php:39`; in-tree capability values `none/vision/image_generation` (grep) and the only `'stability' => 'stable'` — `includes/Features/Image_Generation/Image_Generation.php:46`.
- Abilities: `Title_Generation::register()`/`register_abilities()` with `ability_class` — `includes/Experiments/Title_Generation/Title_Generation.php:53-72`; `Abstract_Ability` abstract `input_schema/output_schema/execute_callback/permission_callback/meta`, `category()` default `WPAI_DEFAULT_ABILITY_CATEGORY`, `guideline_categories()` default `[]`, `get_system_instruction()`/`load_system_instruction_from_file()` (auto-detects only `system-instruction.php`; docblock `:163` still says `prompt.php`; no `prompt.php` exists), `get_ability_slug()` strips `ai/` and `-`→`_`, `wpai_system_instruction` `@since 0.7.0`, scoped filters `@since 1.3.0` — `includes/Abstracts/Abstract_Ability.php:60-230,377-426`; `ai/suggest-reply` (ADMIN) — `includes/Experiments/Suggest_Reply/Suggest_Reply.php:35,47,69-73`; `Content.php` `check_permission()/get_read_content_input_schema()` (three-mode `oneOf`) `/get_read_content_output_schema()` — `:242,1041,1065,1164`; `Users.php` `check_permission()/get_users_input_schema()/get_users_output_schema()` — `:160,785,931`; `Settings.php` `manage_options` — `:206-207`; `Posts.php` `current_user_can( 'edit_post', $post_id )` — `:226-236`; gated wrappers 30–37 lines calling `( new X() )->init()` — `includes/Abilities/Gated/*.php`; `Show_In_Abilities` hooks `register_setting_args`/`register_post_type_args` and patches already-registered `post`/`page` — `includes/Abilities/Show_In_Abilities.php:50-60,173-206`; Core 7.1 has **no** `show_in_abilities` anywhere (grep empty) — the flag is purely the plugin's polyfill; `ability_class` accepted by core — `wp-7.1/wp-includes/abilities-api.php:283`, `abilities-api/class-wp-abilities-registry.php:159-174`; core skips callback validation when `get_class( $this ) !== self::class` — `abilities-api/class-wp-ability.php:301-313`; REST `wp-abilities/v1` + `rest_base 'abilities'` — `rest-api/endpoints/class-wp-rest-abilities-v1-list-controller.php:27,35`; `wp_supports_ai()` = `WP_AI_SUPPORT` + filter only — `wp-7.1/wp-includes/ai-client.php:21-38`; canonical abilities set `'show_in_rest' => true` — e.g. `includes/Abilities/Title_Generation/Title_Generation.php:202-206`.
- Guidelines service (as 1.3.0 code): categories/meta keys/tag names, `DEFAULT_MAX_GUIDELINE_LENGTH 5000`, `is_available()`, method signatures, `wpai_max_guideline_length`/`wpai_use_guidelines` defaults, `<guidelines>\n…\n</guidelines>` wrapper, `_guideline_block_{name with / → _}`, draft-status note, static `reset_cache()` — `includes/Services/Guidelines.php:88-117,147-149,159,188,219-273,283-308,324`; in-tree `guideline_categories()` choices (`['site','copy']` for Title/Excerpt/Summarization/Type Ahead/Suggest Reply/Meta Description/Content Classification, `['site','images']` for the three image abilities, `['site','copy','additional']` for Editorial Notes); `'' !== $instruction` guard first and preamble — `Abstract_Ability.php:177-183`; `Alt_Text_Generation` passes `'alt-text-system-instruction.php'` — `includes/Abilities/Image/Alt_Text_Generation.php:470`.
- Dashboard widgets: `wp_dashboard_setup` (priority 10), `manage_options`, `wpai_status`/`wpai_capabilities` via `wp_add_dashboard_widget()`, `Asset_Loader::enqueue_style( 'dashboard-widgets', 'admin/dashboard' )`, no hooks of its own — `includes/Admin/Dashboard/Dashboard_Widgets.php:57-88`; `get_features_by_stability()` split — `AI_Status_Widget.php:132-133`; `wpai_is_{$slug}_connector_configured( bool, array )` `@since 0.9.0` — `:234`; only in admin — `Main.php:126-130`; 0.8.0 "framework" wording — `CHANGELOG.md:359`.
- Settings import/export: `/settings/export` GET, `/settings/import` POST, `SCHEMA_VERSION = 1`, `manage_options`, `SENSITIVE_PATTERNS = key, token, secret, credential, password, auth` matched as underscore-delimited segments (+optional plural `s`) — `includes/REST/Settings_IO_Controller.php:42-79,96-110,152,354-373`.
- 1.3 migrations/deprecations: `ai_generated→wpai_generated`, `ai_generated_summary→wpai_generated_summary`, comment `ai_note→wpai_note`, existing new key authoritative, stale old rows deleted — `includes/Admin/Upgrades/V1_3_0.php:52-54,72-105`; `AI_Service` `@deprecated 1.3.0` + `_deprecated_class` — `includes/Services/AI_Service.php:31,88`; `get_ai_service()` `_deprecated_function` — `includes/helpers.php:247-252`; `wpai_meta_description_result_temperature` via `apply_filters_deprecated(…,'1.3.0')` with unused result — `includes/Abilities/Meta_Description/Meta_Description.php:308`.
- Embeddings at 1.3.0: `supports_embedding_generation()` = `class_exists( AiClient ) && class_exists( EmbeddingBuilder )`; `generate_embeddings()` → `WP_Error( 'ai_embeddings_unsupported' )` — `includes/helpers.php:823-853`; overlay sentinel is `EmbeddingBuilder` — `includes/SDK_Overlay.php:93-98`; WP 7.1 bundles PHP AI Client 1.3.1 without it.
- Request logging: retention default `0`, cron scheduled only when `> 0` and cleared at `0` — `includes/Logging/AI_Request_Log_Manager.php:71-90,113`; `wpai_request_logged( $log_id = wp_generate_uuid4(), $insert_data )` — `AI_Request_Log_Repository.php:83,131`; `tokens_input/tokens_output/response_preview` columns — `AI_Request_Log_Schema.php:125-133`; `detect_request_kind()` hard-returns `image/metadata/embeddings/audio` first — `Log_Data_Extractor.php:282-318`; providers map from `wp_get_connectors()` (`ai_provider` type) — `:55-77`; sole `new AI_Request_Log_Manager()` — `includes/Experiments/AI_Request_Logging/AI_Request_Logging.php:72`; `log_ai_request()` returns `false` when `Logging_Integration::get_log_manager()` is null, requires `type/operation/status` — `helpers.php:806-814`, `AI_Request_Log_Manager.php:133-170`; all six `@since 1.0.0`; `ai-request-logging` id.
- Other filters: `wpai_default_request_timeout( int, string )` `@since 1.2.0` — `helpers.php:722-735`; readme/changelog say `wp_ai_client_default_request_timeout` — `readme.txt:245`, `CHANGELOG.md:97`; `wpai_has_ai_credentials` `@since 0.7.0`, api_key-only loop — `helpers.php:479-508`; callers (status widget, Settings `hasCredentials`, Comment Moderation) — `AI_Status_Widget.php:65`, `Settings_Page.php:83`, `Comment_Moderation.php:370,917`; `wpai_has_image_generation_support` `@since 1.1.0` — `helpers.php:566-570`; `wpai_bulk_action_max_items` default 100, `max( 1, … )` — `helpers.php:754-756`; `wpai_settings_feature_groups` — `Settings_Page.php:195`; `wpai_feature_{$id}_settings` — `Type_Ahead.php:150`; `wpai_content_translation_languages` behaviour (`sanitize_key`, drop bad labels, ignore non-array) — `includes/Experiments/Content_Translation/Languages.php:74-104`; slug suggestions default 3 clamped 1–10 — `Slug_Generation.php:110-111`; CC `available_terms( terms, taxonomy, strategy )`, `min_confidence` default `MIN_CONFIDENCE` clamped 0–1, `candidate_pool_size` non-positive→100 — `includes/Abilities/Content_Classification/Content_Classification.php:359-366,592-600,721-726`; CC strategy/max re-sanitised — Experiments `Content_Classification.php:284-286,306-310`; alt-text `DEFAULT_DOWNLOAD_TIMEOUT 30`, `20 * MB_IN_BYTES` — `Alt_Text_Generation.php:69,78`; `wpai_generated_image_filename` sanitised, extension appended — `Import_Base64_Image.php:278-284`; `wpai_ability_category( $category_label, $slug )` — `Ability_Handler.php:152`; `wpai_meta_description_meta_key( $key, ?string $plugin_slug )` — `SEO_Integration.php:126`; empty `wpai_meta_description` suppresses output — Experiments `Meta_Description.php:209-213`; `should_moderate` default `toxicity_score >= 0.7 && 'negative'` — `Comment_Moderation.php:396-406`; pills only on `dashboard` screen — `:484-498`; `moderate_guests` default `true` — `:137,359`; `wpai_comment_analysis_result` array short-circuit through `sanitize_analysis_result()` — `Comment_Analysis.php:230-234`; `wpai_remove_data_on_uninstall` per-site on multisite — `includes/Admin/Uninstall.php:61-72,108`; `wpai_summarization_min_content_length` deprecated 1.1.0 — `Summarization.php:196-201`.
- Deprecated shims: 7 filters + 2 actions, `has_filter()/has_action()` guards, `'0.6.0'`, "will be removed in v1.0" — `includes/Deprecated.php:30-195`; per-feature legacy filter — `Abstract_Feature.php:182-188`; preferred-model filters plain `apply_filters`, `@since 0.2.1/0.2.0/0.3.0` — `helpers.php:227,300,342`.
- Version attributions (CHANGELOG.md): 0.6.0 `Abstract_Experiment`→Feature refactor #316 and `WPAI_*` #317 (`:449-466`); 0.8.0 widgets #311, Guidelines #359, `wp_supports_ai()` #268, Image Generation promoted #418 (`:359-366`); 1.0.0 Request Logging #437, Editorial Notes/Updates rename #528 (`:272,284`); 1.0.1 `is_globally_enabled/is_individually_enabled` #604 (`:240`); 1.1.0 `wpai_has_image_generation_support` #748 (`:137`); 1.2.0 Suggest Reply #724, Advanced settings #842, `core/read-content` #739, `core/read-users` #774, timeout #862 (`:91-101`); 1.3.0 Content Translation #747, Slug Generation, Custom Abilities #881, import/export + Site Health #734, #770, meta keys #867, `AI_Service` #905, temperature #913 (`:9-35`). PR titles for #268/#317/#359/#418/#724/#739/#747/#770/#774 match the skill's attributions.
- MCP: `McpAbilityExposure::is_meta_public()` — explicit `meta.mcp.public` wins, else `true === meta.public` — `mcp-adapter-0.6.1/includes/Abilities/McpAbilityExposure.php:42-68`; v0.6.0 release notes: "Abilities with `meta.public: true` are now exposed through the default MCP server unless `meta.mcp.public` explicitly opts out"; `README.md:122-123`.
- Misc: provider plugins register on `init` priority 5 — `provider-anthropic-1.0.4/plugin.php:54`; bulk Alt Text/Summarization nonce checks — `Alt_Text_Generation.php:346`, `Summarization.php:278`; Site Health `debug_information` + `site_status_tests` credential test — `includes/Admin/Site_Health.php:39-40,112-135`; URLs `https://github.com/WordPress/ai` and `https://make.wordpress.org/ai/` — `ai.php:12,18`; Requirements min PHP 7.4 / WP 7.0 — `includes/Requirements.php:26-31`; `WPAI_IS_TEST` asset bypass irrelevant to skill.

## Could not verify

- `references/experiments-framework.md:193` — "The 0.5.0 release notes referenced 'Finalize requirements to elevate an Experiment to a Feature.'" Not in `CHANGELOG.md` 0.5.0 (`:475-492`), `readme.txt`, `docs/`, the GitHub 0.5.0 release page HTML, or a make.wordpress.org/ai site search (GitHub releases API was rate-limited).
- `references/guidelines-integration.md:5` — "those files are unchanged from 23.7.2": no 23.7.2 checkout; supporting evidence only — `gb-23.9.0/changelog.txt` lists every Knowledge/Guidelines storage change (#79149, #79263, #79709 "Add Blocks as a registry scope") under 23.6.0 and none under 23.7.x–23.9.0.
- `references/experiments-framework.md:179` — "`ability_class` … since WordPress 6.9": only the 7.1 package was available; its docblocks carry `@since 6.9.0` with no later `@since` for the argument, but 6.9.0 itself was not inspected.
- `SKILL.md:15` / `references/experiments-framework.md:189` — "Adapter 0.5.0 and earlier read `meta.mcp.public` alone and never inherit": inferred from the v0.6.0 release-note wording ("now exposed"); 0.5.0 code not available.
- `SKILL.md:256` — "Advanced settings … collapsed by default": supported only by `CHANGELOG.md:92,101` (#842); the JS was not inspected.
- The task brief names PR #224, but the skill cites no #224 (its PR references are #268, #317, #359, #418, #724, #739, #747, #770, #774 — all checked) and no Trac tickets.
- GitHub API `/pulls/{n}/files` was rate-limited; the file lists for #988/#975/#976/#1005 were taken from `https://github.com/WordPress/ai/pull/{n}.diff`, and the develop tree from the branch tarball, which are authoritative.
